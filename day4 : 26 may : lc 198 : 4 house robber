lc 198: 4 house robber : here there are array of element is given ... we can;t rob two adjacent house... we can rob alternate houses... rob the house in such a way that the maximum profit can be achevied.

  free = 0 ( we can't rob this house becasue the previous one is already robbed) , free = 1 ( we can either rob or skip this one)

  why dp ? repetation : have to learn for each free 0 / 1 with corresponding element.  2d table having row as ith indexes and coloumn as 0 and 1.

    class Solution {
public:


    int helper_fun(vector<int>&a, int n , int i , int free, vector<vector<int>>&dp){
        if(i == n) return 0;
        if(dp[i][free] != -1){
            return dp[i][free];
        }
        if(free == 0){
            return dp[i][free] = helper_fun(a,n, i+1,1,dp);


        }
        int c1 = a[i]+ helper_fun(a,n,i+1,0,dp);
        int c2 = helper_fun(a,n,i+1,1,dp);

        return dp[i][free] = max(c1,c2);
    }
    int rob(vector<int>& nums) {
        int n = nums.size();
       vector<vector<int>> dp(n, vector<int>(2, -1));

        return helper_fun(nums,n,0,1,dp);
        
    }
};
