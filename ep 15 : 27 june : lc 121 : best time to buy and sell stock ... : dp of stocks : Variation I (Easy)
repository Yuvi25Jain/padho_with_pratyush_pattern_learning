/* BUY AND SELL STOCK I


question: given an array of stock ... we have to buy the stock at the min price and we should get maximum profit possible. */

//approach 1 : for loop : sliding window
// int maxprofit(vector<int>&a){
//     int n = a.size();
//     int maxprf = 0 ;
    
//     for(int i = 0 ; i<n ; i++){
//         for(int j = i+1 ; j<n ; j++){
//             int prf = a[j]-a[i];
//             maxprf = max(maxprf,prf);
//         }
//     }
//     return maxprf;
// }





// approach 2 : recursion  (dp)
// int helper(vector<int>&a , int n , int i , int k){
//     if (i == n){
//         return 0 ;
//     }
//     if (k == 0){
//         return 0 ;
        
//     }
//     else if (k == 2){
//         int c1 = helper(a,n,i+1,k-1)-a[i];
//         int c2 = helper(a,n,i+1,k);
        
//         return max(c1,c2);
//     }
//     else{
//         int c1 = helper(a,n,i+1,k-1)+a[i];
//         int c2 = helper(a,n,i+1,k);
        
//         return max(c1,c2);
//     }
// }



// int maxprofit(vector<int>&a){
//     int n = a.size();
//     int k = 2;
//     int gain = helper(a,n,0,k);
//     return gain;
// }

// approach 3 :  memoization

// int helper(vector<int>&a, int n , int i , int k, vector<vector<int>>&dp){
//     if (i == n) return 0;
//     if(k == 0) return 0;

//     if(dp[i][k] != -1) return dp[i][k];
    
//     else if(k== 2){
//         int c1 = helper(a,n,i+1,k-1,dp) - a[i];
//         int c2 = helper(a,n,i+1,k,dp);
//         return dp[i][k] = max(c1,c2);
//     }
//     else{
//         int c1 = helper(a,n,i+1,k-1,dp) + a[i];
//         int c2 = helper(a,n,i+1,k,dp);
//         return dp[i][k] = max(c1,c2);
        
//     }
// }
 
// int maxprofit(vector<int>&a){
//     int n = a.size();
//     int k = 2;
//     vector<vector<int>>dp(n+1, vector<int>(k+1,-1));
//     return helper(a,n,0,k,dp);
// }


//approach 4 : tabulation
int maxprofit(vector<int>&a){
    int n = a.size();
    int k = 2;
    
    vector<vector<int>>dp(n+1, vector<int>(k+1,0));
    for(int i = n-1 ; i>=0 ; i--){
        for(int moves = 1 ; moves<= k ;moves++){
            if(moves == 2){
                int c1 = dp[i+1][moves-1] - a[i];
                int c2 = dp[i+1][moves];
                dp[i][moves] = max(c1,c2);
            }
            else{
                int c1 = dp[i+1][moves-1] + a[i];
                int c2 = dp[i+1][moves];
                dp[i][moves] = max(c1,c2);
                
            }
        }
    }
    return dp[0][k];
}


int main()
{
    vector<int>a;
    input(a);
    output(a);
    cout<<endl;
    cout<<"Maximum profit possible: "<<maxprofit(a)<<endl;

    return 0;
}
